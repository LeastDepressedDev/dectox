Необходим плагин на VS code для декорирования кода. Что основное в него должно входить:
1) Способность конвертировать квартинки в ASCII рисунки и вставлять их в виде комментария.
2) При нажатии на хоткей, вместо обычного табаплагин должен выровняться по табуляции предыдущей строки(гига полезная штука когда пишешь на C, чтобы не путаться в структурах)
3) [Опционально*] Сделать возможность комментировать в .json файле без поддержки .jsonc. Идея в том чтобы хранить системную версию с коментами, но при сохранении все коменты вырезаются в редактируемом файле.
4) ... *Заказчик* ещё не определился до конца, так что в последствии требуемый функционал может быть расширен.